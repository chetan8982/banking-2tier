name : Building and Pushing for ECR

on:
  workflow_call

jobs:
  working-job:
    runs-on: ubuntu-latest

    steps:
      - name : Echo Message
        run : echo "Build_Pushis working successfully ✅"